import React, {useEffect} from 'react';

const AddPortfResult = () => {

    useEffect(() => {
        window.scrollTo(0, 0)
    }, [])

    return (
        <div></div>
    );
};

export default AddPortfResult;